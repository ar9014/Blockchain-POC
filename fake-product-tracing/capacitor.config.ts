import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'product-tracing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
